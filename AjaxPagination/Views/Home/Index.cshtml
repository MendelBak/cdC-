<h3>Welcome to the Ajax Pagination example page</h3>
<p>This page makes AJAX calls to a SQL Server database based on the three types of input you can choose; Name, Start Date, and End Date. </p>
<p>Since I'm working with a small set of User data I set the pagination to break after only a small number of results have been returned, this can obviously be changed easily.</p>
<br>

<button id="Jquerytestbutton">testbutton</button>

<form action="/FilterUsers" method="post">
<div class="form-inline">
    <div class="form-group">
        <label for="name">Last Name</label>
        <input type="text" name="name" id="name" class="form-control">
    </div>
</form>
    
    <div class="form-group">
        <label for="startDate">Start Date</label>
        <input type="date" name="startDate" id="startDate" class="form-control">
    </div>

    <div class="form-group">
    <label for="endDate">End Date</label>
    <input type="date" name="endDate" id="endDate" class="form-control">
    </div>

</div>
<br>    

<h3>Results</h3>
<h5 id="DisplayPageNumber">Page Number:</h5>
@{
    for(var idx = 1; idx <= ViewBag.TotalUsers; idx++)
    {
        // Pass in the last user that was displayed on this page as well as the number page that you selected.
        // <a href="ChoosePage/@ViewBag.AllUsers[i].Id/@idx">@idx</a>
    }
}

<table class="table">
    <thead>
        <tr>
            <th>User Id</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email Address</th>
            <th>Registration Date</th>
        </tr>
    </thead>
    <tbody>
            @{
                foreach (var user in ViewBag.AllUsers)
                {
                    <tr>
                    <td>@user.Id</td>
                    <td>@user.first_name</td>
                    <td>@user.last_name</td>
                    <td>@user.email</td>
                    <td>@user.created_at</td>
                    </tr>
                }
            }
    </tbody>
</table>

